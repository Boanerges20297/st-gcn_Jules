services:
  telegram-gateway:
    build:
      context: .
      dockerfile: docker/Dockerfile.telegram-gateway
    container_name: report-preview-telegram-gateway
    env_file:
      - .env
    environment:
      PYTHONUNBUFFERED: 1
      REPORTPREVIEW_RUNTIME_DIR: /app/outputs/mempalace/chat
      MEMPALACE_CONTEXT_PATH: /app/.mempalace.md
      STATIC_SCREENSHOT_REPO_DIR: /app/outputs/hermes/screenshot-report_preview
      TELEGRAM_SCREENSHOT_APP_PORT: 4173
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./outputs:/app/outputs
      - ./.mempalace.md:/app/.mempalace.md:ro
      - ./.mempalace:/app/.mempalace:ro
      - ./.hermes.md:/app/.hermes.md:ro
      - ./.gemini:/app/.gemini:ro
      - ./powershell:/app/powershell:ro
      - ./scripts:/app/scripts:ro
    restart: unless-stopped
